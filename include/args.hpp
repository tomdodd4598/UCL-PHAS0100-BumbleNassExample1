#ifndef ARGS_H
#define ARGS_H

#include <tuple>

namespace args {

    /*template<typename... T>
    std::tuple<T...> argv_tuple(int argc, char** argv) {
        std::tuple<T...> tuple;
        for (int i = 1; i < argc; ++i) {
            std::get<i>(tuple) = 
        }
        return tuple;
    }*/
}

#endif
